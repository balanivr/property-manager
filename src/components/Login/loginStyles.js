const styles = theme => ({
    main: {
        marginTop: 64,
        marginBottom: 'calc(64px + 4.5em)',
        display: 'flex',
        alignContent: 'center',
        justifyContent: 'center',
    },
    pageWrapper: {
        maxWidth: 400,
        marginTop: 100
    },
    loginForm: {
        minHeight: 400,
        padding: 18
    }
});
export default styles;